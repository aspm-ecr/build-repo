apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: prod

on:
  workflow_dispatch: {}

jobs:
  deploy:
    steps:
      - uses: cloudbees-io/register-deployed-artifact@v2
        with:
          labels: ecr-go-repo
          artifact-id: f17ebac3-7d8a-446b-b2a9-87cf00ba9b4e
          target-environment: Pre-Prod